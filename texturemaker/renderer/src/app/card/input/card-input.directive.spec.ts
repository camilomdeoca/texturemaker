import { CardInputDirective } from './card-input.directive';

describe('InputNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CardInputDirective();
    expect(directive).toBeTruthy();
  });
});
