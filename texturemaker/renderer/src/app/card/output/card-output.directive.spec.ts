import { CardOutputDirective } from './card-output.directive';

describe('CardOutputDirective', () => {
  it('should create an instance', () => {
    const directive = new CardOutputDirective();
    expect(directive).toBeTruthy();
  });
});
